# Processed data

`source_verses.json` is an exact normalized copy of the selected source after running
`scripts/ingest.py`.

`verses.json` is reserved for the application's canonical corpus. It must not be
populated until Chapter 13 numbering is explicitly resolved and reviewed.
